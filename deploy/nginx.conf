server {
    listen 80;
    server_name gameoflife.example.com;  # Replace with actual domain/IP

    root /var/www/gameoflife;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/javascript application/json;
    gzip_min_length 1000;
    gzip_comp_level 6;

    # Cache static assets with hash (immutable)
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # No cache for index.html (always fetch latest)
    location = /index.html {
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # Fallback to index.html for SPA routing (if needed)
    location / {
        try_files $uri $uri/ /index.html;
    }
}

# HTTPS configuration (uncomment after obtaining SSL certificate with certbot)
# server {
#     listen 443 ssl http2;
#     server_name gameoflife.example.com;
#
#     ssl_certificate /etc/letsencrypt/live/gameoflife.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/gameoflife.example.com/privkey.pem;
#
#     # SSL configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#
#     root /var/www/gameoflife;
#     index index.html;
#
#     # ... (copy all location blocks from HTTP server block)
# }
#
# # Redirect HTTP to HTTPS
# server {
#     listen 80;
#     server_name gameoflife.example.com;
#     return 301 https://$server_name$request_uri;
# }
